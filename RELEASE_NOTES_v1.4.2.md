# StarAPI Router v1.4.2 发行说明

> 发布日期：2026-09-09 ｜ 类型：重大核心功能更新 ｜ 推荐安装包：StarAPI-Setup-1.4.2.exe

---

## 🌟 重磅焦点：全新「路由中心」全面进化

作为 StarAPI Router 最核心的大脑引擎，「路由中心」在本次版本完成了重大架构演进与稳定性强化，彻底实现高可用、高弹性的工业级流量调度：

### 1. 统一模型路由组（Unified Model Routes）
- **多模型聚合为一个接口**：可将火山方舟、阿里百炼、OpenAI、智谱等不同平台的多个同级模型组合为一个虚拟模型（如 `Auto` 或 `旗舰推理`）。
- **客户端零感知调用**：客户端（Cursor、Claude Code、NextChat 等）只需配置一个名字，路由中心在背后智能分摊流量并自动做健康探针和故障转移。
- **能力完整继承**：自动透传上游真实的上下文窗口大小、Thinking 思考层级与视觉支持。

### 2. 跨套餐链路级灾备降级（Fallback Chains）
- **毫秒级自动故障切换**：当主力模型遭遇 429 限流、上游宕机（500/503）或网络波动时，路由中心在毫秒内自动将请求无缝降级转移到备用模型，保障业务永不中断！
- **降级跳数精准控制**：支持自定义最大跳数（Max Hops）与同名互斥死锁防护；支持将整个统一路由组直接挂入降级链节点。

### 3. 智能账号轮换池（Account Rotation）
- **多 Key 自动分流**：同一模型支持绑定多把 API Key，自动平摊并发与额度。
- **缓存优先轮换（Cache-First，省 Token 降延迟）**：会话级 KV-Cache 亲和绑定，确保多轮上下文持续命中上游相同实例，大幅减少重复计费与推理等待。
- **故障熔断与智能冷却**：遇到限流自动进入冷却避让，冷却期满自动复活重试；支持面板一键重置所有凭据冷却。

### 4. 智能视觉路由（Vision Router）
- **让纯文本模型“长出眼睛”**：当向不支持图文理解的纯文本大模型发送图片时，路由中心自动拦截图片并分流至视觉模型进行精准描述，再将文字结果透明喂给主模型。

---

## 🚀 其它核心亮点

### 1. 面板多语言完整支持（10 种国际语言）
- 设置页新增「界面语言」选择器，支持：简体中文、繁體中文、English、日本語、Türkçe、Tiếng Việt、Português、한국어、Русский、العربية。
- 即选即生效，自动记忆偏好；选择阿拉伯语时自动切换为右至左（RTL）镜像布局。

### 2. 会员权益分档管理
- 统一路由组数与轮换组数按免费 / 标准 / 专业 / 旗舰分档解锁；专业版起开启缓存优先轮换，旗舰版尊享无限路由组与一键解冻。

### 3. 总览页聚焦焕新与网络防护
- 状态条与指标卡全面重构精简，右下角专注展示官方动态；新增「网络防护」页面，支持 DNS 与 WebRTC 泄露一键检测与修复。

### 4. 底层硬核稳定性修复
- 解决默认前缀模式下路由报 `503 auth_not_found` 的模型 ID 匹配偏差；
- 内嵌完整 IANA 时区数据库，解决精简系统与 Windows 下时区伪装失效问题；
- 配额查询与执行集群动态对齐，解决限额动态扣费不准问题。

---

## 升级与安装指南

- **安装版**：直接下载 `StarAPI-Setup-1.4.2.exe` 覆盖安装，自动保留既有配置与凭据。
- **免安装便携版**：解压 `StarAPI-Router-v1.4.2-windows-amd64-portable.zip`，双击 `StarAPI Router.exe` 即可使用，含托盘常驻与 WebView2 运行环境。
