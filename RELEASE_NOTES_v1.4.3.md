# StarAPI Router v1.4.3 发行说明

> 类型：功能增强更新 ｜ 推荐安装包：StarAPI-Setup-1.4.3.exe

---

## 🎨 全新能力：多提供商生图，在对话框里直接出图

本次更新打通了 **3 家生图提供商、2 条独立链路**，客户端无需安装任何插件，切换模型即可出图：

- **Google Gemini 对话生图**：`gemini-3.1-flash-image` 走对话接口，在普通聊天窗口直接渲染图片，支持图生图与多轮提示词改图。
- **xAI Grok 图像端点**：`grok-imagine-image`、`grok-imagine-image-quality` 走标准图像接口，画质顶尖、写实度极高，quality 档适合壁纸级生成。
- **ChatGPT 图像端点**：`gpt-image-2.5` 等走标准图像接口，适合矢量图形、概念草稿与商业插画。

> 三家渠道全部在订阅内使用。接入要点：把客户端模型显式切换到生图模型，并**关闭流式输出**，出图最稳定。

---

## 🧭 路由中心增强：降级链更聪明、更可控

### 1. 会话级粘性锁定
新会话严格按你配置的优先级从第 1 位起跑；某个成员成功响应后，即在整个会话生命周期内锁定它，后续请求持续沿用——避免频繁跨模型切换导致的上下文错乱与推理劣化。仅在锁定成员失败时才按顺序降级，并自动把锁定点迁移到新的可用成员。粘性按客户端会话标识严格隔离，无会话标记不锁定；闲置 30 分钟自动释放。

### 2. 真实流量驱动的健康度熔断
链成员的健康状态由真实请求结果驱动：连续失败达阈值即标记不健康并进入冷却，新会话启动时直接跳过故障成员、由第一个健康成员接管。零额外探测成本，也杜绝了合成探测的假阳性误判；冷却期过后自动恢复候选资格。

### 3. 统一路由组「锁定首选通道」
统一模型路由组新增该模式：勾选后组内固定优先使用第一个可用通道，只有它不可用才转移；不勾选则维持跨账号轮询，用轮询分摊配额。

### 4. 降级链自定义触发条件（7 项）
每条降级链可逐项勾选触发条件：`400 参数错误`、`429 频控限流`、`500 服务异常`、`502 网关错误`、`503 不可用`、`504 超时`、`网络故障`，默认全选。解决第三方兼容上游因参数格式不兼容返回 400 时被直接中断、无法自动降级到备选模型的问题。

### 5. 实时调用弹窗
降级链卡片旁新增「实时查看」：内置环形缓冲，弹窗打开后每 2 秒自动刷新，即时展示当前请求模型、最终命中模型、供应商与耗时状态，链路走向一目了然。

### 6. 保存时跳数可达性校验
保存降级链时自动计算链路位置总数，当最大跳数不足以覆盖全部成员时，提示「尾部成员不可达」并提供一键修正按钮——作为成本护栏引导，而非强制拦截。

---

## 🌐 订阅与信息入口

- **配套出口线路**：订阅套餐页新增「配套网络线路」分组，网络防护页新增「配套出口线路」卡片，集中收录长期可用的线路入口，两页数据同步生效。
- **信息中心官方资源入口**：接入地址下方新增官网与使用说明跳转卡片，面板内即可直接查教程与最新动态。

---

## 🔧 稳定性与体验

- 修复聊天生图链路的模型名解析问题，生图模型在聊天窗口调用更稳定。
- 修复已知问题与若干已知 Bug，提升系统整体运行稳定性。

---

## 升级与安装指南

- **安装版**：直接下载 `StarAPI-Setup-1.4.3.exe` 覆盖安装，自动保留既有配置与凭据。
- **免安装便携版**：解压 `StarAPI-Router-v1.4.3-windows-amd64-portable.zip`，双击 `StarAPI Router.exe` 即可使用，含托盘常驻与 WebView2 运行环境。
- **面板内一键升级**：设置页「软件更新」→ 检查更新 → 一键自动升级；或启动时弹窗直接升级，全程自动下载、校验、热替换与平滑重启。
- **下载校验**：全部发行包均附 `checksums.txt`，下载后请先验哈希。
