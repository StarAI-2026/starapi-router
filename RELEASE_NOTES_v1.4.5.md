# StarAPI Router v1.4.5 发行说明

> 类型：功能增强更新 ｜ 推荐安装包：StarAPI-Setup-1.4.5-public.exe

---

## 用量统计：看清「谁在花你的额度」

以前用量页只回答"哪个模型花了多少"，回答不了"是哪台工具在花"。这一版把口径补齐：

- **按客户端识别**：自动判断每个请求来自哪个编码工具（Codex、Claude Code、WorkBuddy、OpenCode 等），不用你手工登记。
- **整页跟着勾选走**：勾选某个客户端后，顶部总数、使用趋势、年度活跃图、模型统计、会话明细都只看它；未识别的流量单独归入一桶，可以手动补一条识别规则，补完之后所有带 User-Agent 的记录都会按新规则重新归类（更早、连 User-Agent 都没存下来的历史只能继续留在未归属桶里）。
- **网关用量 + 工具本机用量合并成一个总数**：直连官方接口、没经过本网关的消耗也能纳进来，同一个工具只按覆盖更全的那一个来源计一次，不重复相加。
- **年度活跃视图与年份切换**、**积分展示**、**模型 Token 占比**：长期用量趋势一眼可见，只报积分不报 Token 的来源显示为「—」而不是 0。

---

## 面板内查看运行日志

设置与诊断页可直接查看后端运行日志与本机工具日志，排查问题不再需要去文件夹里翻文本文件。

---

## 客户端接入更稳

一键安装与更新加了并发保护：同一个客户端不会被两次安装同时写坏，重复点击会收到明确提示而不是静默出错。

---

## 稳定性

修复已知问题与若干体验感知 Bug，提升系统整体运行稳定性。

---

## 升级与安装指南

- **安装包**：下载 `StarAPI-Setup-1.4.5-public.exe` 覆盖安装，自动保留既有配置与凭据。
- **免安装便携版**：解压 `StarAPI-Router-v1.4.5-windows-amd64-portable.zip`，双击 `StarAPI Router.exe`。
- **面板内一键升级**：设置页「软件更新」→ 检查更新。
- **下载校验**：发行包附 `checksums.txt`，下载后请先验哈希。
