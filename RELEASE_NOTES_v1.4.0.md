# StarAPI Router v1.4.0

## 版本核心亮点

StarAPI Router v1.4.0 是一次重大架构与体验升级版本，带来了更加坚固的本地优先 API 路由体验、全新的跨设备配置迁移能力、十款主流客户端的一键启停自愈管理，以及多项底层稳定性和推测解码专项修复。

---

## 详细功能更新

### 1. 账号管家与路由中心体验升级
- **OAuth 复制链接与手动核验**：OAuth 登录卡片新增「复制授权链接」按钮，生成可复制的授权 URL 与一次性设备码，支持在无头服务器、局域网远程端或任意外部浏览器中完成授权；新增「我已授权，核验」手动确认按钮，授权完毕即刻刷新保存。
- **凭据快速录入与健康状态显性化**：新增「+ 粘贴录入凭据」弹窗，支持直接粘贴单条 Token / API Key 或完整 JSON 凭据，系统自动按提供商包装入库；列表卡片新增「403 权限异常/封禁」与「临时冷却中」等健康状态视觉徽标与详细错误原因展示。
- **模型正则通配映射（Regex Rewrite）**：模型别名映射层支持正则表达式通配模式（如 `regex:gpt-4.*` 或 `^gpt-4.*` 统一定向至指定后端落地模型），同时严格保证思考后缀（Thinking suffix）不丢失、精确匹配优先。

### 2. GitHub Device Flow 与跨设备配置迁移
- **安全免密 GitHub Device Flow 登录**：账号登录全面切换为安全规范的 GitHub Device Authorization Flow，面板展示一次性校验码并轮询完成登录，令牌继续仅加密存储于本机，彻底避免任何 Client Secret 泄露风险。
- **全配置跨设备导出与一键导入（.sapi）**：会员中心新增「配置文件导出 / 导入」区块。用户只需设定一个密码，即可将本地套餐、统一 API Key、视觉模型和代理模型通过 PBKDF2（210k 轮）+ AES-256-GCM 强加密导出为 `.sapi` 备份文件；在新电脑上选文件输入密码即可完整还原；VIP 会员支持推送到 GitHub 私有 Gist 实现跨设备一键云同步。

### 3. 十大客户端全面接入与自愈管理
- **工作目录选择器直接内嵌**：终端客户端（Codex / Claude Code / Gemini / Grok Build / PI / OpenCode / Hermes / deepseek-harness 等）点「启动」时，目录选择器直接内嵌在网格正下方并平滑滚入可视区，彻底告别全屏遮罩割裂感。
- **一键启停与实时进程监控**：卡片右下角直接展示运行状态与实时进程数（如「重启 25」），未运行客户端直接提供「启动」副按钮；Hermes/OpenCode 桌面版通过 GUI 模式独立重启，不再误开黑窗口终端。
- **客户端专属代理与路径探测**：支持为每个客户端配置独立的 HTTP/SOCKS 代理地址与可执行文件绝对路径；优化冷启动分发机制，彻底解决后台静默启动 CLI 导致的闪退问题。

### 4. 托盘与侧边栏服务自愈保障
- **托盘与侧边栏一键重启后台服务**：无需打开任务管理器结束进程，通过互斥恢复锁与优雅关闭流程快速重启 `server.exe`；通过 `instance_id` 确定性检测新进程状态，避免网络请求刷屏报错。
- **关键配置变动智能提醒**：新增配置快照比对能力，仅当 `host`/`port`/`tls`/`auth-dir` 等必须重启的核心字段变化时才在侧边栏亮起琥珀色重启提示，套餐、密钥、模型的实时热加载不再被打扰。

### 5. 模型生态、规则引擎与插件沙箱
- **模型一致性测试实验室**：对指定套餐进行协议符合度与能力盲测核对，报告自动脱敏，根据选测题目自适应计算单次测试的最佳输出预算与超时控制。
- **自定义路由规则引擎**：支持结构化路由条件匹配（如套餐 RPM 使用率 ≥ 80% 自动熔断导流至备选模型），支持灰度比例调控与一键版本回滚。
- **跨套餐降级链（旗舰特性）**：支持在 429/5xx 时按显式配置的备选链路顺序安全转移请求。
- **用户插件权限沙箱与版本防降级**：用户插件默认置于只读和通知层，禁止触碰本地认证文件与执行系统命令；安装插件时严格对比已装版本，拦截恶意降级（`plugin_downgrade_blocked`）。

---

## 修复的缺陷与 Bug

1. **终端 CLI 客户端启动闪退**：修复了在 Windows 下启动无控制台进程因标准输入重定向为 NUL 导致的闪退 Bug，改为链式拉起独立可见终端窗口。
2. **PI 客户端配置文件误写**：纠正了旧版将模型列表误写入 PI 动态缓存 `models-store.json`（导致模型被 PI 自动覆写丢失）的 Bug，改为规范写入官方 `~/.pi/agent/models.json` 并自动清理历史残留。
3. **退出 StarAPI 自动还原客户端配置生效**：修复退出还原请求使用加密密钥原文被 401 拦截的缺陷，新增 Localhost sidecar 安全凭据校验分支，保证退出 StarAPI 后彻底还原 Codex、Claude Code 等本地环境变量与配置文件。
4. **llama.cpp 推测解码（MTP）空转修复**：修复 llama.cpp 0.3.0+ 因 `--spec-type` 默认为 none 导致推测解码完全失效的缺陷，自动注入 `--spec-type draft-mtp`；对无 MTP 层的 GGUF 模型自动跳过参数，避免显存分配异常闪退；KV 量化全面支持 K 与 V 精度独立下拉调节。
5. **GitHub 备份 401 错误提示修正**：解决当 GitHub 账号缺少 Gist 权限时报错被转换为 502 的误导问题，明确提示重新授权 Gist 范围。
6. **模型检测整型除零崩溃**：修复极端异常探针返回导致除零触发服务端 panic 的问题，完善连接复用与流式判定。

---

## 下载与分发资产说明

请在下方 Assets 区域下载与您系统架构匹配的发布包：

- **Windows 用户**：
  - 常见 64 位电脑（Intel/AMD）：下载 `StarAPI_1.4.0_windows_amd64.zip`（解压即可使用）。
  - ARM 架构设备（如骁龙芯片笔记本）：下载 `StarAPI_1.4.0_windows_aarch64.zip`。
- **macOS 用户**：
  - Apple Silicon 芯片（M1/M2/M3/M4 系列）：下载 `StarAPI_1.4.0_darwin_aarch64.tar.gz`。
  - Intel 处理器机型：下载 `StarAPI_1.4.0_darwin_amd64.tar.gz`。
- **Linux / FreeBSD 用户**：
  - 标准 Linux：下载 `StarAPI_1.4.0_linux_amd64.tar.gz` 或 `StarAPI_1.4.0_linux_aarch64.tar.gz`（基于 GLIBC 2.17 基准构建，支持动态插件）。
  - 软路由 / 嵌入式 / OpenWrt 等 musl 系统：下载 `no-plugin` 便携版（如 `StarAPI_1.4.0_linux_amd64_no-plugin.tar.gz`）。
  - FreeBSD 系统：下载 `freebsd_amd64` 或 `freebsd_aarch64_no-plugin`。
- **完整性校验**：
  - 每个发布文件对应的 SHA-256 校验哈希均记录在 `checksums.txt` 中。

> Public source archive contains only public docs.
